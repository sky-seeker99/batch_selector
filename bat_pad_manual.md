# バッチ実行パッド

---

## 1. ソフトの紹介

本プログラムは、「バッチ実行セレクター」の補助ツールとして、「バッチ実行セレクター」から起動されるプログラムです。

バッチの内容は「バッチ実行セレクター」で指定され、小さいウィンドウに表示されます。

このウィンドウにファイルをドラッグ＆ドロップすることで、指定したバッチを連続実行できます。

※文字が表示されている下側の部分をつかんで移動することはできません。上側をドラッグしてウィンドウを移動するか、メニューの「label」で文字を非表示にしてから移動してください。

---

# 2. メニューについて

## (1) バッチの実行

ダブルクリックすると、バッチを実行します。

## (2) ドラッグ＆ドロップ

ファイルをドラッグ＆ドロップすると、バッチを実行します。

複数のファイルをドラッグ＆ドロップすると、ファイル数分だけバッチを実行します。

## (3) 右クリックメニュー

右クリックすると、メニューが表示されます。

以下にメニューについて説明します。

- **exec** … バッチを実行します。
- **view** … バッチの内容を表示します。
- **label** … ウィンドウに表示されている文字の表示／非表示を切り替えます。
- **stay** … ウィンドウを常に最前面に表示します。
- **para** … ファイル処理を並列で実行します。
- **hide** … 実行ウィンドウを非表示にします。
- **shell** … バッチ実行を、バッチ実行パッドとは別のプロセスで実行するか、子プロセスで実行するかを選択します。
- **font** … フォントを変更します。
- **backcolor** … 背景色を変更します。
- **version** … バージョンを表示します。
- **quit** … プログラムを終了します。
- **quit（次回起動）** … プログラムを終了します。ただし、次回に「バッチ実行セレクター」を起動したときに、一緒に起動します。

---

# 3. 履歴

**Ver 0.90**　初公開！

**Ver 0.91**　quit（次回起動）を追加

**Ver 0.92**
- メニューに「exec」の項目を追加
- ラベル部分をダブルクリックしても実行できるようにした

**Ver 0.93**　Windows終了時の処理を追加

**Ver 0.94**　バッチ実行セレクターが終了したとき、同時に終了するようにした

**Ver 0.95**　バグフィックス

**Ver 0.96**　hide機能を追加





