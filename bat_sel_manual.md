# バッチ実行セレクター

---

## 1. ソフトの紹介

ドラッグ＆ドロップで指定したバッチファイルを連続実行します。

バッチはファイルに記述することもできますが、GUI画面上で複数のバッチファイルを管理しながら、記述することもできます。

---

## 2. 使用方法

プログラムを実行すると、「bat_sel」ウィンドウが表示されます。

### (1) バッチファイルを作る

ウィンドウの中央にポインタを移動し、右クリックします。

メニューから［insert］を選択し、左クリックします。

ウィンドウの上側に「input」ウィンドウが表示され、バッチのファイル名を入力します。

ここでは「sample」と入力し、「OK」ボタンを押します。

「bat_sel」ウィンドウに「sample」が表示されます。

「sample」を左クリックで選択し、上側の［○］ボタンを押します。

すると、「editor」ウィンドウが表示されます。

「editor」に以下の内容を入力してください。

```bat
dir "%1"
pause
```

「bat_sel」ウィンドウの「sample」をダブルクリックすると、このバッチファイルが実行されます。

このとき、`%1` はNULLになります。

次に、ファイルを「bat_sel」ウィンドウにドラッグ＆ドロップしてください。

すると、今度は `%1` にファイルのパス名が入り、バッチが実行されます。

※ドラッグ＆ドロップした位置がリストの場合は、上記のようにコマンドを実行します。  
editor画面にドラッグ＆ドロップした場合は、ファイルのフルパスをeditorに取り込みます。

---

### (2) バッチ内で記述できる特殊文字

`%1` ：ドラッグ＆ドロップしたファイルのフルパスが代入されます。

`%r` ：ドラッグ＆ドロップしたファイルのドライブのみが代入されます。

`%d` ：ドラッグ＆ドロップしたファイルのディレクトリのみが代入されます。

`%f` ：ドラッグ＆ドロップしたファイルの、拡張子を除いたファイル名のみが代入されます。

`%e` ：ドラッグ＆ドロップしたファイルの拡張子のみが代入されます。

`\` ：コマンドの最後尾に付けると、次の行と連結します。

`%2～%9` ：ドラッグ＆ドロップしたファイルのフルパスで、2番目～9番目のファイル名が代入されます。  
※`%2～%9`を指定した場合は連続実行されませんので、注意してください。

`!#file_begin ファイル名` ：この文字列以降の行から、指定したファイル名で内容が書き出されます。

`!#file_end` ：`!#file_begin` の指定範囲の終わりを示します。

`!#proc プログラム 引数1 引数2～` ：プログラムを別プロセスで起動します。

ExcelなどのGUIプログラムを起動するとき、通常の記述ではDOS窓が残ってしまう場合があります。その場合は、この `!#proc` を使用してください。

---

### ① 特殊文字の使用例

ドラッグ＆ドロップしたファイルが、

```text
C:\Documents and Settings\kasuya99\デスクトップ\bat_sel.csv
```

だとします。

`%1` は、

```text
C:\Documents and Settings\kasuya99\デスクトップ\bat_sel.csv
```

になります。

`%r` は、

```text
C:
```

になります。

`%d` は、

```text
\Documents and Settings\kasuya99\デスクトップ\
```

になります。

`%f` は、

```text
bat_sel
```

になります。

`%e` は、

```text
csv
```

になります。

※`%e`、`%r`、`%f`、`%d`を無効にしたい場合は、`!#parainh` を記述してください。

例えば、以下のようになります。

```bat
echo off
!#parainh
echo %date%
```

上記のバッチを実行すると、日付が表示されます。

`!#parainh` がない場合は、`%d` がディレクトリとして認識されるため、

```text
ate
```

と表示されます。

---

### ② コマンドの連結

以下のように記述した場合、

```bat
echo aaa bbb\
ccc\
ddd\
```

次のように実行されます。

```bat
echo aaa bbb ccc ddd
```

---

### ③ 複数ファイルをドラッグ＆ドロップした場合

以下の3つのファイルをドラッグ＆ドロップしたとします。

```text
C:\Documents and Settings\kasuya99\デスクトップ\bat_sel0.csv
C:\Documents and Settings\kasuya99\デスクトップ\bat_sel1.csv
C:\Documents and Settings\kasuya99\デスクトップ\bat_sel2.csv
```

以下のように記述すると、

```bat
echo file1=%1
echo file2=%2
echo file3=%3
```

次のように実行されます。

```bat
echo file1=C:\Documents and Settings\kasuya99\デスクトップ\bat_sel0.csv
echo file2=C:\Documents and Settings\kasuya99\デスクトップ\bat_sel1.csv
echo file3=C:\Documents and Settings\kasuya99\デスクトップ\bat_sel2.csv
```

---

### ④ `!#file_begin`、`!#file_end` の使用例

以下のように記述した場合、

```bat
echo aaa
!#file_begin c:\csv\aaa.txt
abcdef
eeeeee
!#file_end
pause
```

次のように実行されます。

```bat
echo aaa
echo abcdef > c:\csv\aaa.txt
echo eeeeee >> c:\csv\aaa.txt
pause
```

---

### ⑤ `!#proc` の使用例

以下のように記述した場合、

```bat
!#proc "C:\Program Files\OpenOffice.org 2.4\program\scalc.exe"
```

次のように実行されます。

```bat
start "bat selector" "C:\Program Files\OpenOffice.org 2.4\program\scalc.exe"
```

---

## (3) 本プログラムが作成するファイル

本プログラムは、プログラムが存在するディレクトリに「bat_sel.csv」というファイルを作成します。

このファイルには、ウィンドウの現在の状態に関する情報が格納されています。

```text
#group#グループ名.csv
```

には、バッチ名の一覧情報が格納されています。

```text
#bat#グループ名#バッチ名.csv
```

には、バッチの内容が格納されています。

---

# 3. 「bat_sel」のボタンとメニューについて

## (1) バッチの実行

バッチ名をダブルクリックするか、［Enter］キーを押すと、バッチを実行します。

## (2) ドラッグ＆ドロップ

ファイルをドラッグ＆ドロップすると、バッチを実行します。

複数のファイルをドラッグ＆ドロップすると、ファイル数分だけバッチを実行します。

## (3) 右クリックメニュー

右クリックすると、メニューが表示されます。

以下にメニューについて説明します。

- **execute** … バッチを実行します。ファイル名はNULLになります。
- **batch pad** … よく使用するバッチに対して、バッチパッドを別ウィンドウで表示します。バッチパッドについては「bat_pad_manual」を参照してください。
- **job enable** … リモート実行受付の有効／無効を指定します。
- **job menu** … リモート実行時にキューに登録されているジョブ一覧を表示します。
- **rename** … バッチ名を変更します。
- **insert** … バッチを新規に挿入します。
- **stay** … ウィンドウを常に最前面に表示します。
- **para** … ファイル処理を並列で実行します。
- **shell** … バッチ実行を、バッチ実行セレクターとは別のプロセスで実行するか、子プロセスで実行するかを選択します。
- **hide** … 実行ウィンドウを非表示にします。
- **font** … フォントを変更します。
- **backcolor** … 背景色を変更します。
- **load file** … 全バッチデータをファイルからロードします。
- **save file** … 全バッチデータをファイルへ保存します。
- **version** … バージョンを表示します。

## (4) ボタンについて

- **［←］** … 「editor」ウィンドウを閉じます。
- **［→］** … 「editor」ウィンドウを表示します。
- **［∽］** … グループを変更します。
- **［＿］** … タスクトレイに格納します。
- **［↑］** … バッチ名を上に移動します。
- **［↓］** … バッチ名を下に移動します。
- **［×］** … 選択したバッチを削除します。
- **［◎］** … バッチを実行します。
- **×スイッチ** … スイッチをONにしてドラッグ＆ドロップすると、バッチを実行するのと同等のバッチを登録します。

---

# 4. タスクトレイ

## (1) 左クリック

左クリックで、メニューの表示／非表示を切り替えます。

## (2) 右クリック

右クリックすると、メニューが表示されます。

以下にメニューについて説明します。

- **Position Reset** … ウィンドウ位置を左上に移動します。ウィンドウが画面外に出てしまった場合に使用します。
- **Show** … バッチ実行パッドを表示します。
- **Exit** … プログラムを終了します。

---

# 5. リモートJOB実行

外部プログラムから、バッチ実行セレクターに登録されているバッチを実行する機能です。

実行するまでの待ち時間を指定できます。

付属のコンソールプログラム「bat_sel_remote.exe」で実行します。

## (1) バッチの実行

バッチを実行します。

書式：

```text
bat_sel_remote.exe exec JOB名 グループ名 バッチの項目名 ウエイト時間(sec) [パラメータ1] [パラメータ2] ・・・ [パラメータn]
```

例：

```text
bat_sel_remote.exe exec job_test default file_ctrl 15
bat_sel_remote.exe exec job_test default file_ctrl 15 p0 p1 p2 p3
```

---

## (2) バッチのキャンセル

待機状態にあるバッチをキャンセルします。

書式：

```text
bat_sel_remote.exe kill JOB名
```

例：

```text
bat_sel_remote.exe kill job_test
```

---

## (3) 全バッチデータをファイルからロード

全バッチデータをファイルからロードします。

書式：

```text
bat_sel_remote.exe config ファイル名
```

例：

```text
bat_sel_remote.exe config menu_data.csv
```

---

## (4) 全バッチデータをファイルへ書き出す

全バッチデータをファイルへ書き出します。

書式：

```text
bat_sel_remote.exe config_write ファイル名
```

例：

```text
bat_sel_remote.exe config_write menu_data.csv
```

---

## (5) プログラム実行後にJOBを削除しない（オプション）

ジョブ実行時、通常はJOBを削除しますが、このオプションを指定するとJOBを削除しません。

バッチの実行（`exec`）の前に指定してください。

書式：

```text
bat_sel_remote.exe own_kill
```

例：

```text
bat_sel_remote.exe own_kill
bat_sel_remote.exe exec job_test default file_ctrl 15
```

この場合、バッチ「file_ctrl」の最後に、自分のJOBを削除する以下の記述を入れてください。

```text
bat_sel_remote.exe kill job_test
```

削除するJOB名を引数に指定する場合の例：

遠隔起動側：

```text
bat_sel_remote.exe own_kill
bat_sel_remote.exe exec job_test default file_ctrl 15 job_test
```

バッチの最後：

```text
bat_sel_remote.exe kill %2
```

---

## (6) 他のJOBを待つ（オプション）

他のJOBが終了するのを待ってからJOBを実行する場合のオプションです。

バッチの実行（`exec`）の前に指定してください。

書式：

```text
bat_sel_remote.exe wait JOB名
```

例：

```text
bat_sel_remote.exe own_kill
bat_sel_remote.exe exec job_test1 default aaa 0 a0 job_test1

bat_sel_remote.exe wait job_test1
bat_sel_remote.exe own_kill
bat_sel_remote.exe exec job_test2 default aaa 0 b0 job_test2

bat_sel_remote.exe wait job_test2
bat_sel_remote.exe exec job_test3 default bbb 0 c0
```

上記は、`job_test1` のバッチ実行が完了した後に `job_test2` が実行され、`job_test2` のバッチ実行が完了した後に `job_test3` が実行されるサンプルです。

---

## (7) すべてのバッチをキャンセル

待機状態にあるすべてのバッチをキャンセルします。

書式：

```text
bat_sel_remote.exe kill_all
```

例：

```text
bat_sel_remote.exe kill_all
```

---

## (8) JOBリストをファイルに出力

JOBリストをファイルに出力します。

書式：

```text
bat_sel_remote.exe job_list ファイル名
```

例：

```text
bat_sel_remote.exe job_list c:\job.txt
```

---

# 6. JOBメニューについて

## (1) JOBメニューの表示

「bat_sel」を右クリックして表示されるメニューから［job menu］を実行すると、JOBメニューのウィンドウが開きます。

## (2) 右クリックメニュー

右クリックすると、メニューが表示されます。

以下にメニューについて説明します。

- **job timer start** … JOB TIMERを再開します。
- **job timer stop** … JOB TIMERを停止します。
- **job delete** … 選択したJOBを削除します。（JOB TIMERを停止してから選択してください。）
- **job exec** … 選択したJOBを実行します。（JOB TIMERを停止してから選択してください。）
- **job all delete** … JOBをすべて削除します。
- **color up** … 上側の背景色を変更します。
- **color down** … 下側の背景色を変更します。
- **font up** … 上側のフォントを変更します。
- **font down** … 下側のフォントを変更します。

---

---

# 7. 履歴

**Ver 0.90**　初公開！

**Ver 0.91**　ダブルクォーテーション対策

**Ver 0.92**　インターフェースを全面変更

**Ver 0.93**
- StayOnTop時にドラッグ＆ドロップできなくなるバグを修正
- デフォルトカラーのバグを修正

**Ver 0.94**
- タスクバーに表示されないようにした
- バッチ実行時に、本プログラムがバッチの終了を待機しないようにした
- 複数ファイルを並列で処理できるようにした

**Ver 0.95**　タスクトレイに常駐できるようにした

**Ver 0.96**
- アイコンを変更
- タスクトレイの左クリックの機能を変更

**Ver 0.97**　文字列両端の半角スペースを削除しないようにした

**Ver 0.98**
- キーリストでバッチを実行できるようにした
- バッチの内容を表示／非表示できるようにした

**Ver 0.99**　全面書き換え

**Ver 0.99a**　バグフィックス

**Ver 0.99b**
- エディタ側のフォントとバックカラーを変更可能にした
- グループ名の欄の幅を文字数に合わせて可変にした
- バッチ内にダブルクォーテーションが含まれている場合に削除されてしまうバグを修正

**Ver 0.99c**
- 旧コンフィグファイルがある場合にバックアップコピーするようにした
- ドラッグ＆ドロップでバッチを生成できるようにした
- 複数選択でバッチを実行できるようにした

**Ver 0.99d**　共通のDLLファイルを使用する必要をなくした

**Ver 0.99e**　`%e` のバグフィックス

**Ver 0.99f**
- コマンド文字列の最後に `\` を付けると、次の行と連結するようにした
- ファイル保存関係のバグを修正

**Ver 1.00**　`!#file_begin`、`!#file_end`、`%2～%9` を追加

**Ver 1.01**　全バッチデータを一つのCSVファイルにロード／セーブできるようにした

**Ver 1.01b**　画面関係を修正

**Ver 1.01c**
- `!#proc` を追加
- Editorにファイルをドラッグ＆ドロップしたとき、ファイル名に空白がある場合はダブルクォーテーションで囲むようにした
- よく使用するバッチに対して、バッチパッドを別ウィンドウで表示するようにした

**Ver 1.01d**
- 起動したバッチ実行パッドを記憶するようにした
- 次回起動時に、指定されたバッチパッドのみ同時に起動するようにした

**Ver 1.01e**　バッチ実行パッドを表示したときに、コンフィグレーションファイルを更新するようにした

**Ver 1.01f**　Windows終了時の処理を追加

**Ver 1.02**　バッチ実行パッドを別プログラムにせず、プログラム内に統合した

**Ver 1.03**　メモリリークのバグを修正

**Ver 1.04**
- バッチ実行パッドを別プログラムにした
- 終了時にプロセス間通信を使用して終了するようにした

**Ver 1.05**
- 「save file」のバグを修正
- フォントスタイルのバグを修正
- ［Enter］キーで実行できるようにした

**Ver 1.06**　他のプログラムからリモート実行できるようにした

**Ver 1.06a**　リモートプログラムのインターフェースを変更

**Ver 1.07**
- リモートプログラムのインターフェースを変更
- パラメータを受け取れるようにした

**Ver 1.08**
- リモートプログラムのインターフェースを追加
- リモート実行受付の有効／無効を指定できるようにした

**Ver 1.09**　JOBメニューからJOBを制御できるようにした

**Ver 1.10**
- 画面構成を変更
- リモートプログラムのインターフェース（config、config_write）を追加

**Ver 1.11**　エディタ画面がない場合でも拡大・縮小できるようにした

**Ver 1.12**
- エディタ画面を開く／閉じるときの左側の位置を変更
- エディタ画面を開いているときにファイルをドラッグ＆ドロップすると、ファイルパスをエディタに追加するようにした

**Ver 1.13**　リモートプログラムのインターフェース（own_kill、wait）を追加

**Ver 1.14**
- 右クリックメニューに「hide」を追加
- リモートプログラムのインターフェース（kill_all、job_list）を追加

**Ver 1.15**　画面関係を修正

**Ver 1.16**
- 画面位置のリセット機能を追加
- ドラッグ＆ドロップした位置によって処理を変更

**Ver 1.17**　JOB OWN削除時のバグを修正

**Ver 1.18**
- JOBを使用したとき、バッチファイル名が重複して誤動作する場合があるバグを修正
- 下地のエンジンを最新のものに変更

**Ver 1.19**　Shutdownしたときにエラーが発生するバグを修正

**Ver 1.20**　`!#parainh` を追加

**Ver 1.21**　File Save／Loadにバグがあったため修正

**Ver 1.21a**　インストーラーの圧縮方式を変更

**Ver 1.21b**　圧縮方式をインストーラーなしに変更

